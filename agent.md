# Agent Rules

1. **Explicit Permission for Git Operations**: Do not commit or push changes without explicit permission from the user. Even if a plan was approved, ask for final confirmation before running `git commit` or `git push`.
